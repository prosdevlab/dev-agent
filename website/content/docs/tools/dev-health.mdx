# dev_health (removed)

`dev_health` has been merged into [`dev_status`](/docs/tools/dev-status).

Use `dev_status` with `section="health"` for health checks:

```
dev_status(section: "health")
```

This returns the same Antfly connectivity and repository access checks that `dev_health` provided.
