packages:
  - 'packages/*'

# pnpm >=10 blocks dependency build scripts by default. These native deps
# need their postinstall to run (esbuild binary, @parcel/watcher native addon)
# for the build and file watcher to work in CI and locally.
onlyBuiltDependencies:
  - '@parcel/watcher'
  - esbuild
